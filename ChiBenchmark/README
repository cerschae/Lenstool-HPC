The folder lenstool chi has the src code for a chibenchmark version of lenstool

running the following commands will create in the src folder the lenstool executable
./configure
make

the benchmark parameter file "mod.par" is in the /lenstoolchi/examples/Jauzac folder
once in the corresponding folder
the following commands will run the benchmark

export OMP_NUM_THREADS=[Number of threads you want to use, default is 8]
../../src/lenstool mod.par
